@model IEnumerable<e_okul.Models.Ogretman>

@{
    ViewData["Title"] = "Index";
}

 

<p>
    <a class="btn btn-primary" asp-action="Create">Ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soyadi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Soyadi)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.OgretmenId">Güncelle</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.OgretmenId">Detay</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.OgretmenId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
